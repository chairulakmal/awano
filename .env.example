# Copy to .env and adjust if needed:
#   cp .env.example .env

# Prisma / Next.js (local Docker Postgres from docker-compose.yml)
DATABASE_URL="postgresql://awano:awano@localhost:5433/awano?schema=public"

# Auth.js v5 — generate with: openssl rand -base64 32
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"
