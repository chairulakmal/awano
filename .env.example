# Copy to .env and adjust if needed:
#   cp .env.example .env

# Prisma / Next.js. Any PostgreSQL 18 reachable at this URL works; see docs/dev.md.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/awano?schema=public"

# Auth.js v5: generate with: openssl rand -base64 32
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"
